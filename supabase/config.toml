# Proiectul Supabase de producție NOD BIM (nou, creat de proprietar).
# Folosit DOAR de Supabase CLI pentru migrări locale/deploy (`supabase link`,
# `supabase db push`). La runtime aplicația citește exclusiv variabilele de
# mediu din Cloudflare (SUPABASE_URL / SUPABASE_SECRET_KEY), nu acest fișier.
project_id = "laqznjcfbcsfktvbrdeg"
